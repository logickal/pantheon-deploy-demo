# Pantheon Environment Variables
# These are automatically provided by the Pantheon platform in production.
# No need to set these locally for development.

# Cache Configuration (Pantheon provides these automatically)
# CACHE_BUCKET=<gcs-bucket-name>
# OUTBOUND_PROXY_ENDPOINT=<proxy-endpoint>

# Node Environment (set in project.toml)
# NODE_ENV=production

# Optional: Enable cache debugging
# CACHE_DEBUG=true
